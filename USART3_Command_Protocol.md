# USART3 舵机控制命令协议说明

## 概述
本系统通过USART3接收舵机控制命令，支持控制两个舵机（地址分别为2和3）移动到指定位置。

## 命令格式

### 帧结构（7个字符）
```
M[ID][±][AAAA]
```

### 字段说明

| 字符位置 | 字段名 | 说明 | 取值范围 |
|---------|--------|------|----------|
| 1 | Header | 命令头 | 固定字符 'M' |
| 2 | Motor_ID | 电机地址 | '2' 或 '3' |
| 3 | Sign | 符号位 | '1' = 正, '0' = 负 |
| 4-7 | Position | 位置值（4位数字） | '0000' - '9999' |

### 位置参数
- 位置值为4位数字字符串，范围 0000-9999
- 符号位 '1' 表示正数，'0' 表示负数
- 实际位置 = Sign=='1' ? Position : -Position
- 例如: "1" + "0500" = +500, "0" + "1000" = -1000

## 舵机配置

- **舵机1地址**: 2
- **舵机2地址**: 3

## 命令示例

### 示例1：控制舵机2移动到位置+500

**命令**: `M210500`

- M: 命令头
- 2: 电机地址2
- 1: 正数
- 0500: 位置值500

### 示例2：控制舵机3移动到位置-1000

**命令**: `M301000`

- M: 命令头
- 3: 电机地址3
- 0: 负数
- 1000: 位置值1000

### 示例3：控制舵机2移动到原点(0)

**命令**: `M210000` 或 `M200000`

- 原点可以用正数或负数表示，都是0

### 示例4：控制舵机3移动到位置+9999（最大值）

**命令**: `M319999`

- M: 命令头
- 3: 电机地址3
- 1: 正数
- 9999: 位置值9999

## Python 测试代码示例

```python
import serial
import time

def send_servo_command(ser, motor_id, position):
    """
    发送舵机控制命令
    :param ser: 串口对象
    :param motor_id: 电机ID (2 或 3)
    :param position: 目标位置 (-9999 到 9999)
    """
    # 确保motor_id在有效范围内
    if motor_id not in [2, 3]:
        print(f"错误：电机ID {motor_id} 无效！只支持ID 2 和 3")
        return False
    
    # 确保position在有效范围内
    if position < -9999 or position > 9999:
        print(f"错误：位置 {position} 超出范围！(-9999 到 9999)")
        return False
    
    # 确定符号位和绝对值
    if position >= 0:
        sign = '1'
        abs_pos = position
    else:
        sign = '0'
        abs_pos = -position
    
    # 构建命令字符串: M[ID][Sign][AAAA]
    # 位置值格式化为4位数字，不足补0
    command = f"M{motor_id}{sign}{abs_pos:04d}"
    
    # 发送命令
    ser.write(command.encode('ascii'))
    print(f"✓ 发送命令: {command}")
    print(f"  电机ID: {motor_id}, 目标位置: {position:+d}")
    return True

# 使用示例
if __name__ == "__main__":
    # 打开串口（根据实际情况修改端口号和波特率）
    port = "COM3"  # Windows
    # port = "/dev/ttyUSB0"  # Linux
    baudrate = 115200
    
    try:
        ser = serial.Serial(port, baudrate, timeout=1)
        print(f"串口 {port} 已打开，波特率 {baudrate}")
        
        # 测试：控制舵机2移动到位置+500
        send_servo_command(ser, 2, 500)
        time.sleep(1)
        
        # 测试：控制舵机3移动到位置-1000
        send_servo_command(ser, 3, -1000)
        time.sleep(1)
        
        # 测试：控制舵机2回到原点
        send_servo_command(ser, 2, 0)
        time.sleep(1)
        
        # 测试：控制舵机3移动到最大值
        send_servo_command(ser, 3, 9999)
        
        ser.close()
        print("串口已关闭")
        
    except Exception as e:
        print(f"错误: {e}")
```

## 串口调试助手测试

使用串口调试助手时，请以**ASCII文本**方式发送数据。

### 配置
- **端口**: 根据实际情况选择
- **波特率**: 115200
- **数据位**: 8
- **停止位**: 1
- **校验位**: 无

### 测试步骤
1. 打开串口
2. 选择"**文本发送**"模式（ASCII）
3. 输入命令（例如：`M210500`）
4. 点击发送

### 常用命令快速参考

| 命令 | 说明 |
|------|------|
| `M210000` | 舵机2移动到位置0（原点） |
| `M210500` | 舵机2移动到位置+500 |
| `M211000` | 舵机2移动到位置+1000 |
| `M200500` | 舵机2移动到位置-500 |
| `M201000` | 舵机2移动到位置-1000 |
| `M310000` | 舵机3移动到位置0（原点） |
| `M310500` | 舵机3移动到位置+500 |
| `M311000` | 舵机3移动到位置+1000 |
| `M300500` | 舵机3移动到位置-500 |
| `M301000` | 舵机3移动到位置-1000 |
| `M319999` | 舵机3移动到最大位置+9999 |
| `M309999` | 舵机3移动到最小位置-9999 |

## 注意事项

1. **位置范围**: 
   - 命令中的位置值为4位数字（0000-9999）
   - 加上符号位，实际范围为 -9999 到 +9999
   - 请确保位置值在舵机的有效运动范围内

2. **命令格式**:
   - 命令总长度固定为7个字符
   - 所有字符必须是ASCII字符
   - 位置值必须是4位数字，不足补0（例如：5应写成0005）

3. **通信稳定性**:
   - 连续发送命令时建议添加延时（至少100ms）
   - 确保RS485总线终端电阻正确配置

4. **错误处理**:
   - 无效的电机ID（非2或3）将被忽略
   - 无效的符号位（非0或1）将被忽略
   - 位置包含非数字字符将被忽略

5. **电源要求**:
   - 确保舵机电源充足，避免电压波动
   - STM32和舵机建议使用独立电源，共地

## 系统初始化

系统启动时会自动：
1. 初始化USART3（波特率115200）
2. 启动DMA接收模式
3. 设置舵机原点（如果已启用）

## 故障排查

### 问题1：发送命令后舵机无响应
- 检查串口连接是否正常
- 确认波特率设置为115200
- 验证命令格式是否正确（7个字符，M开头）
- 检查舵机ID是否为2或3
- 使用串口监视工具确认数据已发送

### 问题2：舵机移动异常
- 确认位置值是否在合理范围内（-9999到+9999）
- 检查符号位是否正确（1为正，0为负）
- 检查RS485总线连接
- 验证舵机电源是否正常

### 问题3：命令偶尔失效
- 确保命令是完整的7个字符
- 检查位置字段是否为4位数字（例如5应该写成0005）
- 增加命令发送间隔时间
- 避免在命令字符串中包含额外的空格或换行符
