# UART6 视觉误差命令快速参考

## 命令格式
```
E[X_Sign][XXXX][Y_Sign][YYYY]
```

## 字段说明
| 位置 | 字段 | 说明 | 示例 |
|------|------|------|------|
| 1 | E | 命令头（固定） | E |
| 2 | X_Sign | X轴符号 | 1=正, 0=负 |
| 3-6 | XXXX | X轴误差（4位） | 0000-9999 |
| 7 | Y_Sign | Y轴符号 | 1=正, 0=负 |
| 8-11 | YYYY | Y轴误差（4位） | 0000-9999 |

## 常用命令示例

### 基本位置
```
E1000010000  - 目标居中（X=0, Y=0）
E1050010000  - 目标在右侧500px
E0050010000  - 目标在左侧500px
E1000010500  - 目标在上方500px
E1000000500  - 目标在下方500px
```

### 四个角落
```
E1050010500  - 右上角 (X=+500, Y=+500)
E0050010500  - 左上角 (X=-500, Y=+500)
E0050000500  - 左下角 (X=-500, Y=-500)
E1050000500  - 右下角 (X=+500, Y=-500)
```

### 典型跟踪场景
```
E1010000200  - 轻微偏右下 (X=+100, Y=-200)
E0030001500  - 偏左上 (X=-300, Y=+1500)
E1080011200  - 显著偏右上 (X=+800, Y=+1200)
```

## 坐标系
```
        Y+ (上)
         |
         |
X- ------+------ X+ (右)
(左)     |
         |
        Y- (下)
```

## Python快速发送
```python
import serial

ser = serial.Serial('COM6', 115200)

# 发送命令
def send(x, y):
    x_s = '1' if x >= 0 else '0'
    y_s = '1' if y >= 0 else '0'
    cmd = f"E{x_s}{abs(x):04d}{y_s}{abs(y):04d}"
    ser.write(cmd.encode())
    print(cmd)

# 示例
send(500, 1200)   # X=+500, Y=+1200
send(-300, -800)  # X=-300, Y=-800
send(0, 0)        # 居中

ser.close()
```

## 串口配置
- **端口**: COM6 (根据实际修改)
- **波特率**: 115200
- **数据位**: 8
- **停止位**: 1
- **校验位**: 无
- **模式**: ASCII文本

## 测试工具
```bash
python test_pid_control.py
```

## PID参数（默认）
```
Kp = 1.0    # 比例系数
Ki = 0.01   # 积分系数
Kd = 0.5    # 微分系数
```

## 常见错误
| 错误 | 原因 | 正确示例 |
|------|------|----------|
| `E1500` | 位数不足 | `E1050010000` |
| `E150010000` | 缺少Y符号 | `E1050010000` |
| `E1+50010000` | 符号错误 | `E1050010000` |
| `e1050010000` | 大小写错误 | `E1050010000` |

## 调试提示
1. 使用串口监视器查看发送的命令
2. 阶跃响应测试可用于PID调参
3. 圆形/方形轨迹测试验证跟踪性能
4. 初始PID参数可能需要根据实际调整
